// Tipos de dados em JavaScript

// Numbers
const temperatura = 15;
const altura = 1.79;

// Strings
const carro = "Palio";
const comida = "Arroz";

// Booleans - variáveis que só assumem verdadeiro ou falso
const verdadeiro = true;
const falso = false;

// Exemplos de tipos:
let str = "string";          // string
let num = 21;                // number
let bol = true;              // boolean
let n = null;                // null (typeof retorna "object")
let un;                      // undefined
let fuc = (a) => { return a }; // function
let obj = {};                // object